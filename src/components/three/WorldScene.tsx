"use client";

/* ─── Ultra-Minimal Pure Dark Canvas (No Grid, No 3D Geometry) ─────────────── */
export default function WorldScene({ section }: { section: number }) {
  // Pure background canvas — no 3D objects or grids
  return null;
}
